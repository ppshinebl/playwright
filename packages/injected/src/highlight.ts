class Highlight {
    private disableTooltips: boolean;

    constructor(disableTooltips: boolean = false) {
        this.disableTooltips = disableTooltips;
        // Other constructor logic...
    }

    updateHighlight() {
        // Existing logic...
        if (!this.disableTooltips) {
            // Logic to create tooltips...
        }
    }
}